apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ include "data-proxy.fullname" . }}-publisher
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "data-proxy.labels" . | nindent 4 }}
    app.kubernetes.io/component: publisher
spec:
  scaleTargetRef:
    name: {{ include "data-proxy.fullname" . }}-publisher
    kind: Deployment
    apiVersion: apps/v1
  pollingInterval: {{ .Values.publisher.keda.pollingInterval }}
  cooldownPeriod: {{ .Values.publisher.keda.cooldownPeriod }}
  minReplicaCount: 0
  maxReplicaCount: 1
  triggers:
    - type: redis-streams
      authenticationRef:
        name: {{ include "data-proxy.fullname" . }}-valkey-trigger-auth
      metadata:
        address: {{ .Release.Name }}-valkey.{{ .Release.Namespace }}.svc.cluster.local:6379
        stream: dp:publish
        consumerGroup: publishers
        lagCount: "1"
    - type: redis-streams
      authenticationRef:
        name: {{ include "data-proxy.fullname" . }}-valkey-trigger-auth
      metadata:
        address: {{ .Release.Name }}-valkey.{{ .Release.Namespace }}.svc.cluster.local:6379
        stream: dp:publish
        consumerGroup: publishers
        pendingEntriesCount: {{ .Values.publisher.keda.pendingEntriesCount | quote }}
