apiVersion: v1
kind: Secret
metadata:
  name: {{ include "data-proxy.fullname" . }}-cnpg-dataproxy
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "data-proxy.labels" . | nindent 4 }}
type: kubernetes.io/basic-auth
stringData:
  username: {{ .Values.postgres.db.user | quote }}
  password: {{ .Values.postgres.password | quote }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "data-proxy.fullname" . }}-cnpg-authenticator
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "data-proxy.labels" . | nindent 4 }}
type: kubernetes.io/basic-auth
stringData:
  username: {{ .Values.auth.authenticatorRole | quote }}
  password: {{ .Values.postgres.authenticatorPassword | quote }}
