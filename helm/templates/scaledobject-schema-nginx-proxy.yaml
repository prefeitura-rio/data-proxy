{{- if .Values.ha.enabled }}
{{- $root := . }}
{{- range $schema, $_ := (default $root.Values.cnpg.schemas $root.Values.syncConfig.schemas) }}
---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ include "data-proxy.fullname" $root }}-{{ $schema | replace "_" "-" }}-nginx-proxy
  namespace: {{ $root.Release.Namespace }}
spec:
  scaleTargetRef:
    name: {{ include "data-proxy.fullname" $root }}-{{ $schema | replace "_" "-" }}-nginx-proxy
    kind: Deployment
    apiVersion: apps/v1
  minReplicaCount: {{ $root.Values.ha.fallback.minReplicaCount }}
  maxReplicaCount: {{ $root.Values.ha.fallback.maxReplicaCount }}
  pollingInterval: {{ $root.Values.ha.fallback.pollingInterval }}
  cooldownPeriod: {{ $root.Values.ha.fallback.cooldownPeriod }}
  triggers:
{{ include "data-proxy.haTriggers" (dict "root" $root "schema" $schema "component" "fallback") | nindent 4 }}
{{- end }}
{{- end }}
