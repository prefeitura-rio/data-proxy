apiVersion: keda.sh/v1alpha1
kind: ScaledJob
metadata:
  name: {{ include "data-proxy.fullname" . }}-finalizer
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "data-proxy.labels" . | nindent 4 }}
    app.kubernetes.io/component: finalizer
spec:
  jobTargetRef:
    parallelism: 1
    completions: 1
    activeDeadlineSeconds: {{ .Values.finalizer.keda.activeDeadlineSeconds }}
    backoffLimit: 3
    template:
      metadata:
        labels:
          {{- include "data-proxy.selectorLabels" . | nindent 10 }}
          app.kubernetes.io/component: finalizer
      spec:
        restartPolicy: Never
        serviceAccountName: {{ include "data-proxy.serviceAccountName" . }}
        containers:
          - name: finalizer
            image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
            imagePullPolicy: {{ .Values.image.pullPolicy }}
            command: ["python", "-m", "dp.sync.finalizer"]
            env:
              {{- include "data-proxy.appEnv" . | nindent 14 }}
            volumeMounts:
              {{- include "data-proxy.syncConfigVolumeMount" . | nindent 14 }}
        volumes:
          {{- include "data-proxy.syncConfigVolume" . | nindent 10 }}
  pollingInterval: {{ .Values.finalizer.keda.pollingInterval }}
  minReplicaCount: 0
  maxReplicaCount: 1
  scalingStrategy:
    strategy: accurate
  triggers:
    - type: redis-streams
      authenticationRef:
        name: {{ include "data-proxy.fullname" . }}-valkey-trigger-auth
      metadata:
        address: {{ .Release.Name }}-valkey.{{ .Release.Namespace }}.svc.cluster.local:6379
        stream: dp:sync:finalize
        consumerGroup: finalizers
        lagCount: "1"
