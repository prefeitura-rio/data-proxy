{{- if .Values.ha.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "data-proxy.pgbouncerRoName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "data-proxy.labels" . | nindent 4 }}
data:
  pgbouncer.ini: |
    {{ include "data-proxy.pgbouncerRoConfig" . | nindent 4 }}
{{- end }}
