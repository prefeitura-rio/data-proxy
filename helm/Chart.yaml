apiVersion: v2
name: data-proxy
description: Data proxy — BigQuery → GCS → pg_duckdb sync pipeline with PostgREST and row-level security.
type: application
version: 1.47.0
appVersion: "1.0.0"
dependencies:
  - name: valkey
    version: "0.17.0"
    repository: "oci://registry-1.docker.io/cloudpirates"
    condition: valkey.enabled
    alias: valkey
